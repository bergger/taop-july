#include "stdio.h"

int StrToInt(const char *str);

int main(int argc, char const *argv[])
{
  char str[] = "123";

  printf("%i\n", StrToInt(str));
  return 0;
}

int StrToInt(const char *str) {
  int result;

  return result;
}